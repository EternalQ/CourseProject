@{
    ViewData["Title"] = "Account";
}

<ul class="nav nav-tabs">
    <li class="nav-item">
        <button class="nav-link" asp-action="AccManager" asp-controller="Account">Account</button>
    </li>
    <li class="nav-item">
        <button class="nav-link active">Collections</button>
    </li>
</ul>
<div>
    <h2 class="h4 mt-3 mb-3">Collections manger</h2>
    <div>
        <div>
            <button type="button" class="btn btn-dark" data-toggle="modal" data-target="#addFormModal">
                Add Collection
            </button>

            <div class="modal fade" id="addFormModal" tabindex="-1" role="dialog" aria-labelledby="addFormModalTitle"
                aria-hidden="true">
                <div class="modal-dialog modal-dialog-centered" role="document">
                    <div class="modal-content">
                        <div class="modal-header">
                            <h5 class="modal-title" id="addFormLongTitle">Modal title</h5>
                            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                                <span aria-hidden="true">&times;</span>
                            </button>
                        </div>
                        <div class="modal-body">
                            <form action="post" asp-action="" asp-controller="">
                                
                            </form>
                        </div>
                        <div class="modal-footer">
                            <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
                            <button type="button" class="btn btn-dark">Add collection</button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
<script>

</script>
}